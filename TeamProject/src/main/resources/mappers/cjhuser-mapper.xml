<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mappers.cjhuser-mapper">
	<select id="selectUser" resultType="CjhUserVo">
		select * from tbl_usertest
		where u_id = #{u_id}
	</select>
	
	<select id="selectMemberByIdAndPw" resultType="CjhUserVo">
		select * from tbl_usertest where u_id = #{u_id} and u_pw = #{u_pw}
	</select>
	
	<update id="updateUser">
		update tbl_usertest set
		    u_pw = #{u_pw},
		    u_name = #{u_name},
		    u_email = #{u_email},
		    u_address = #{u_address}
		where u_id = #{u_id}
	</update>
	
</mapper>
