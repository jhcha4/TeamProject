<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mappers.cjhpoint-mapper">
	<update id="usePoint">
		update tbl_usertest set
			u_point = (select u_point from tbl_usertest
					   where u_id = #{u_id}) - #{totalPrice}
		where u_id = #{u_id}
	</update>
	
	<update id="getPoint">
		update tbl_usertest set
			u_point = (select u_point from tbl_usertest
					   where u_id = #{u_id}) + #{totalPrice}
		where u_id = #{u_id}
	</update>
	
	<insert id="plusPoint">
		insert into tbl_point (point_num, point_code, u_id, point_value)
		values (seq_point_num.nextval, 1002, #{u_id}, #{totalPrice})
	</insert>
	
	<insert id="minusPoint">
		insert into tbl_point (point_num, point_code, u_id, point_value)
		values (seq_point_num.nextval, 1003, #{u_id}, #{totalPrice})
	</insert>
	
	<select id="getTotalPoint" resultType="CjhPointVo">
		select point_value from tbl_point
		where u_id = #{u_id} and point_code = 1002
	</select>
</mapper>
